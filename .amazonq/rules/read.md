Read and write to .windows/winbuild.md, lets plan on building this on windows native, lets not use or go the route of wsl, find each issue, break it to small parts, plan on fixes and write it down to winbuild.md to track and then update after we fix. update the winbuild.md of what was done after few steps and call out next steps, so we can pick this up at a later time, keep what we have done before, the idea to preserve that for someone else to redo on their own system, you are running on windows, but before you can run any commands related to the build you need to start the commands with "powershell.exe" then the rest of the command eg "powershell.exe cargo build".